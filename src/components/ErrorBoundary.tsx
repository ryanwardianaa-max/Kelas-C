import { Component,type ErrorInfo,type ReactNode } from 'react'
type State={failed:boolean}
export default class ErrorBoundary extends Component<{children:ReactNode},State>{state:State={failed:false};static getDerivedStateFromError():State{return{failed:true}}componentDidCatch(error:Error,info:ErrorInfo){console.error('KelasKu UI error',error,info.componentStack)}render(){if(!this.state.failed)return this.props.children;return <main className="error-page"><section className="panel error-card"><small>APLIKASI TETAP AMAN</small><h1>Terjadi kendala tak terduga</h1><p>Data lokal Anda tidak dihapus. Muat ulang aplikasi atau kembali ke Beranda untuk mencoba lagi.</p><div className="form-actions"><button className="primary" onClick={()=>window.location.reload()}>Muat Ulang Aplikasi</button><button onClick={()=>{window.history.replaceState(null,'','/');this.setState({failed:false})}}>Kembali ke Beranda</button></div></section></main>}}
