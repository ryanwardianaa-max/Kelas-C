import { Pencil,Plus,Trash2 } from './Icons';import { useState } from 'react';import { COURSE_SCHEDULE,courseName } from '../lib/mockData';import type { Task } from '../types';import { validDate } from '../lib/storage';import Modal from './Modals/Modal'
const blank=():Task=>({id:crypto.randomUUID(),title:'',courseCode:COURSE_SCHEDULE[0].code,description:'',dueAt:'',completed:false,createdAt:new Date().toISOString()})
export default function TasksView({tasks,setTasks}:{tasks:Task[];setTasks:(v:Task[])=>void}){const[edit,setEdit]=useState<Task|null>(null),[filter,setFilter]=useState('aktif');const visible=tasks.filter(t=>filter==='semua'||(filter==='selesai'?t.completed:!t.completed));const submit=(e:React.FormEvent)=>{e.preventDefault();if(!edit)return;setTasks(tasks.some(x=>x.id===edit.id)?tasks.map(x=>x.id===edit.id?edit:x):[...tasks,edit]);setEdit(null)};return <><div className="page-title action-title"><div><small>PRODUKTIVITAS</small><h1>Tugas & deadline</h1><p>Data tersimpan otomatis pada perangkat ini.</p></div><button className="primary" onClick={()=>setEdit(blank())}><Plus/> Tambah tugas</button></div><div className="chips">{[['aktif','Belum selesai'],['selesai','Selesai'],['semua','Semua']].map(([v,l])=><button className={filter===v?'active':''} onClick={()=>setFilter(v)} key={v}>{l}</button>)}</div><section className="panel">{visible.length?visible.map(t=><article className="row task-row" key={t.id}><input type="checkbox" checked={t.completed} onChange={()=>setTasks(tasks.map(x=>x.id===t.id?{...x,completed:!x.completed}:x))}/><div><b>{t.title}</b><small>{courseName(t.courseCode)} · {t.description||'Tanpa catatan'}</small></div><time>{validDate(t.dueAt)?new Date(t.dueAt).toLocaleString('id-ID'):'Deadline tidak valid'}</time><button className="icon" onClick={()=>setEdit(t)}><Pencil/></button><button className="icon danger" onClick={()=>confirm('Hapus tugas ini?')&&setTasks(tasks.filter(x=>x.id!==t.id))}><Trash2/></button></article>):<div className="empty"><h3>Belum ada tugas</h3><p>Catat deadline pertamamu tanpa data simulasi.</p><button onClick={()=>setEdit(blank())}>+ Tambah tugas</button></div>}</section>{edit&&<Modal title={tasks.some(x=>x.id===edit.id)?'Edit tugas':'Tambah tugas'} onClose={()=>setEdit(null)}><form onSubmit={submit}><label>Judul<input required value={edit.title} onChange={e=>setEdit({...edit,title:e.target.value})}/></label><label>Mata kuliah<select value={edit.courseCode} onChange={e=>setEdit({...edit,courseCode:e.target.value})}>{COURSE_SCHEDULE.map(c=><option value={c.code} key={c.code}>{c.name}</option>)}</select></label><label>Deadline<input type="datetime-local" required value={edit.dueAt} onChange={e=>setEdit({...edit,dueAt:e.target.value})}/></label><label>Catatan<textarea value={edit.description} onChange={e=>setEdit({...edit,description:e.target.value})}/></label><div className="form-actions"><button type="button" onClick={()=>setEdit(null)}>Batal</button><button className="primary">Simpan</button></div></form></Modal>}</>}
