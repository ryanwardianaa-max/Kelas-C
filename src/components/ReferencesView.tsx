import { Copy,ExternalLink,Pencil,Plus,Trash2 } from './Icons';import { useState } from 'react';import { COURSE_SCHEDULE,courseName } from '../lib/mockData';import type { ReferenceCategory,ReferenceItem } from '../types';import Modal from './Modals/Modal'
const categories:ReferenceCategory[]=['Jurnal','E-Book','Website','Drive/File','Video','Catatan'];const blank=():ReferenceItem=>({id:crypto.randomUUID(),title:'',courseCode:'UMUM',category:'Website',urlOrPath:'',description:'',tags:[],createdAt:new Date().toISOString()})
export default function ReferencesView({items,setItems}:{items:ReferenceItem[];setItems:(v:ReferenceItem[])=>void}){const[edit,setEdit]=useState<ReferenceItem|null>(null),[course,setCourse]=useState('SEMUA'),[cat,setCat]=useState('SEMUA');const shown=items.filter(x=>(course==='SEMUA'||x.courseCode===course)&&(cat==='SEMUA'||x.category===cat));const save=(e:React.FormEvent)=>{e.preventDefault();if(!edit)return;setItems(items.some(x=>x.id===edit.id)?items.map(x=>x.id===edit.id?edit:x):[...items,edit]);setEdit(null)};return <><div className="page-title action-title"><div><small>REFERENCE VAULT</small><h1>Referensi serbaguna</h1><p>Kelompokkan jurnal, buku, tautan, file, video, dan catatan.</p></div><button className="primary" onClick={()=>setEdit(blank())}><Plus/> Tambah referensi</button></div><div className="filter-bar"><select value={course} onChange={e=>setCourse(e.target.value)}><option value="SEMUA">Semua mata kuliah</option><option value="UMUM">Umum</option>{COURSE_SCHEDULE.map(c=><option value={c.code} key={c.code}>{c.name}</option>)}</select><select value={cat} onChange={e=>setCat(e.target.value)}><option value="SEMUA">Semua kategori</option>{categories.map(c=><option key={c}>{c}</option>)}</select></div><div className="card-grid">{shown.length?shown.map(r=><article className="panel card" key={r.id}><small>{r.category} · {courseName(r.courseCode)}</small><h3>{r.title}</h3><p>{r.description||'Tanpa deskripsi'}</p><div className="tags">{r.tags.map(t=><span key={t}>#{t}</span>)}</div><footer>{r.urlOrPath&&<><a className="button" href={r.urlOrPath} target="_blank" rel="noreferrer"><ExternalLink/> Buka</a><button className="icon" title="Salin URL" onClick={()=>navigator.clipboard.writeText(r.urlOrPath)}><Copy/></button></>}<button className="icon" onClick={()=>setEdit(r)}><Pencil/></button><button className="icon danger" onClick={()=>confirm('Hapus referensi?')&&setItems(items.filter(x=>x.id!==r.id))}><Trash2/></button></footer></article>):<div className="panel empty"><h3>Belum ada referensi</h3><p>Simpan referensi pertamamu atau ubah filter.</p><button onClick={()=>setEdit(blank())}>+ Tambah referensi</button></div>}</div>{edit&&<Modal title="Referensi" onClose={()=>setEdit(null)}><form onSubmit={save}><label>Judul<input required value={edit.title} onChange={e=>setEdit({...edit,title:e.target.value})}/></label><label>Mata kuliah<select value={edit.courseCode} onChange={e=>setEdit({...edit,courseCode:e.target.value})}><option value="UMUM">Umum</option>{COURSE_SCHEDULE.map(c=><option value={c.code} key={c.code}>{c.name}</option>)}</select></label><label>Kategori<select value={edit.category} onChange={e=>setEdit({...edit,category:e.target.value as ReferenceCategory})}>{categories.map(c=><option key={c}>{c}</option>)}</select></label><label>URL / lokasi<input value={edit.urlOrPath} onChange={e=>setEdit({...edit,urlOrPath:e.target.value})}/></label><label>Deskripsi<textarea value={edit.description} onChange={e=>setEdit({...edit,description:e.target.value})}/></label><label>Tags (pisahkan koma)<input value={edit.tags.join(', ')} onChange={e=>setEdit({...edit,tags:e.target.value.split(',').map(x=>x.trim()).filter(Boolean)})}/></label><div className="form-actions"><button type="button" onClick={()=>setEdit(null)}>Batal</button><button className="primary">Simpan</button></div></form></Modal>}</>}
